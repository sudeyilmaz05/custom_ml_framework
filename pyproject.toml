[build-system]
requires = ["setuptools>=61.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "custom_ml_framework"
version = "0.1.0"
description = "A modular ML preprocessing and feature engineering library for YTÜ YZM1022."
authors = [{name = "Your Name"}]
dependencies = [
    "numpy>=1.20.0",
    "pandas>=1.3.0"
]

[tool.setuptools.packages.find]
where = ["."]